---
title: Button
description: A versatile button component with multiple variants, sizes, and states
---

import { Callout } from "fumadocs-ui/components/callout"
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { TypeTable } from 'fumadocs-ui/components/type-table';
import { Button } from "@helgadigitals/vera-ui"
import { ComponentPreview, AutoExtractPreview } from '@/components/component-preview';
import { 
  ButtonUsageExample,
  ButtonVariantsExample,
  ButtonSizesExample,
  ButtonIconExample,
} from '@/components/form-components-examples/button-examples';
import { PlusIcon } from "lucide-react"

# Button

A fundamental interactive element that triggers an action when activated. The Button component supports multiple variants, sizes, and states to fit different use cases in your application.

## Installation

<Tabs groupId="package" items={['pnpm', 'npm', 'yarn']}>
    <Tab value="pnpm" >
      ```bash
      pnpm add @helgadigitals/vera-ui
      ```
    </Tab>
    <Tab value="npm" >
      ```bash
      npm install @helgadigitals/vera-ui
      ```
    </Tab>
    <Tab value="yarn" >
      ```bash
      yarn add @helgadigitals/vera-ui
      ```
    </Tab>
</Tabs>
## Usage

<AutoExtractPreview 
  source="components/form-components-examples/button-examples.tsx" 
  functionName="ButtonUsageExample"
>
  <ButtonUsageExample/>
</AutoExtractPreview>

## Examples

### Variants

<AutoExtractPreview 
  source="components/form-components-examples/button-examples.tsx" 
  functionName="ButtonVariantsExample"
>
  <ButtonVariantsExample/>
</AutoExtractPreview>

### Sizes

<AutoExtractPreview 
  source="components/form-components-examples/button-examples.tsx" 
  functionName="ButtonSizesExample"
>
  <ButtonSizesExample/>
</AutoExtractPreview>

## API Reference

### Props

The Button component accepts all standard HTML button attributes plus these additional props:

<TypeTable
  type={{
    variant: {
      type: '"default" | "destructive" | "outline" | "secondary" | "ghost" | "link"',
      default: '"default"',
      description: 'The visual style variant of the button',
    },
    size: {
      type: '"default" | "sm" | "lg" | "icon"',
      default: '"default"',
      description: 'The size of the button',
    },
    asChild: {
      type: 'boolean',
      default: 'false',
      description: 'When true, the button will render as its child element',
    },
    disabled: {
      type: 'boolean',
      default: 'false',
      description: 'Whether the button is disabled',
    },
    className: {
      type: 'string',
      description: 'Additional CSS classes',
    },
  }}
/>

## Accessibility

The Button component follows WAI-ARIA guidelines:

- **Keyboard Navigation**: Fully accessible via keyboard (Enter/Space to activate)
- **Screen Readers**: Proper semantic button element with accessible text
- **Focus Management**: Clear focus indicators and focus trapping when needed
- **Disabled State**: Properly communicated to assistive technologies

<Callout type="info">
  **ARIA Labels**: For icon-only buttons, always provide an `aria-label` or `aria-describedby` attribute for screen readers.
</Callout>

## Icon
<AutoExtractPreview 
  source="components/form-components-examples/button-examples.tsx" 
  functionName="ButtonIconExample"
>
  <ButtonIconExample/>
</AutoExtractPreview>
